[project]
name = "terminal-note"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pylint>=3.3.7",
    "ruff>=0.11.12",
]

[tool.uv]
package = true


[project.scripts]
tni = "backend.service_inline_note:main"
tno = "backend.service_edit:main"
